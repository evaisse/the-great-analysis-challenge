---
name: 2026-03-25.harness-docker-cleanup
summary: "Investigated leaked Docker containers created by the test harness, created a dedicated GitHub issue and fix branch, removed currently leaked chess containers, and prepared a tooling fix to make cleanup deterministic."
description: "The user asked to audit, clean up, and fix OrbStack container leaks caused by the Docker-backed test harness. I confirmed the leak source in tooling/chess.ts, created GitHub issue #154, created a dedicated fix worktree, removed currently leaked chess containers, and am implementing deterministic cleanup and validation for the harness lifecycle."
agent: codex
version: 1.0.0
model: azure/gpt-5.4
config: "reasoning-mode"
created_at_utc: 2026-03-25T21:53:14Z
updated_at_utc: 2026-03-26T10:28:55Z
---

## 2026-03-25T21:53:14Z
- Actor: user
- Intent: Ask for the full follow-up: audit the leaking workflow, clean up current containers, and patch the source.
- Message: "1, 2, et 3"
- Action: Started a tracked workflow covering issue resolution, cleanup, code fix, and validation.
- Outcome: The work was split into issue/worktree setup, audit, targeted cleanup, and implementation.
- Next: Resolve an issue and move the fix into an isolated worktree.

## 2026-03-25T21:53:14Z
- Actor: tool
- Intent: Discover whether a matching GitHub issue or PR already exists and gather branch context.
- Message: "Listed open issues and PRs, checked the repository default branch, and confirmed GitHub CLI authentication."
- Action: Used `gh auth status`, `gh issue list --state open`, `gh pr list --state open`, and `gh repo view --json defaultBranchRef --jq '.defaultBranchRef.name'`.
- Outcome: No existing issue or PR covered the Docker leak. The default branch is `main`, and authentication is available.
- Next: Create a dedicated issue and fix branch.

## 2026-03-25T21:53:14Z
- Actor: tool
- Intent: Create a dedicated GitHub issue, branch, and worktree for the Docker cleanup fix.
- Message: "Created issue #154, assigned it, created the `fix/154-test-harness-docker-container-cleanup` branch in a dedicated sibling worktree, and posted a kickoff issue comment."
- Action: Used `gh issue create`, `gh issue edit 154 --add-assignee @me`, `git worktree add -b fix/154-test-harness-docker-container-cleanup ...`, and `gh issue comment 154 --body-file ...`.
- Outcome: Issue URL is `https://github.com/evaisse/the-great-analysis-challenge/issues/154`, and the fix worktree is `/Users/evaisse/Sites/projects/the-great-analysis-challenge__feat-83-pgn-parser-variation-tree__fix-154-test-harness-docker-container-cleanup`.
- Next: Audit the harness code from the clean issue branch and remove currently leaked containers.

## 2026-03-25T21:57:06Z
- Actor: tool
- Intent: Remove currently leaked chess containers and verify the immediate cleanup impact.
- Message: "Removed the visible leaked chess containers that were still present in OrbStack, with a couple of expected 'No such container' responses for already-gone entries."
- Action: Used `docker rm -f wonderful_spence relaxed_bell crazy_einstein serene_chatelet objective_hugle || true` followed by a filtered `docker ps -a` verification.
- Outcome: `wonderful_spence`, `relaxed_bell`, and `serene_chatelet` were deleted; `crazy_einstein` and `objective_hugle` were already absent by the time cleanup ran.
- Next: Implement deterministic container naming and explicit stop/remove handling in `tooling/chess.ts`, then validate with local tests and a harness run.

## 2026-03-25T21:59:41Z
- Actor: agent
- Intent: Implement the minimal harness fix that prevents orphaned Docker containers after interrupted or forced termination paths.
- Message: "Added deterministic harness container names, process-level cleanup registration, explicit `docker rm -f` cleanup, and a `finally`-guard around harness execution plus focused tests for naming behavior."
- Action: Updated `tooling/chess.ts` to track active Docker-backed harness containers, assign `--name`, clean them up on exit and stop paths, and ensure `tester.stop()` always runs; added `test/chess.test.ts` for container naming helpers.
- Outcome: The harness now has explicit container ownership and cleanup logic instead of depending only on Docker's `--rm` behavior.
- Next: Run Bun tests, run a focused Docker-backed harness validation, then commit, push, and open the PR.

## 2026-03-25T22:01:12Z
- Actor: tool
- Intent: Validate the cleanup fix with repo-local tests and a focused Docker-backed harness execution.
- Message: "Installed Bun dependencies in the new worktree, ran the tooling test suite successfully, and executed a focused PHP harness test while checking for leftover `tgac-harness-*` containers afterward."
- Action: Used `bun install`, `bun test ./test/*.test.ts`, and `./workflow test-harness --impl implementations/php --track v1 --docker-image chess-php --test \"Invalid Move Detection\" && docker ps -a --filter name='tgac-harness-' --format '{{.Names}}\t{{.Status}}'`.
- Outcome: All 22 Bun tests passed, the focused harness test passed, and no leftover `tgac-harness-*` containers were listed after the run.
- Next: Inspect the final diff, create an atomic commit with memory updates included, merge `origin/main`, push the branch, and open the PR.

## 2026-03-26T10:23:27Z
- Actor: user
- Intent: Ask for a follow-up audit of other Docker entrypoints and for a safe maintenance cleanup command.
- Message: "ok vas-y"
- Action: Performed a targeted repository-wide search for Docker entrypoints and compared them with the live local Docker inventory.
- Outcome: The main remaining non-documentation runtime risk inside this repository is now limited; the only stateful Docker operations in shared tooling are `docker create` + `docker cp` in `tooling/error-analysis.ts`, and that flow already removes the temporary container in a `finally` block. The remaining `docker run` occurrences are mostly documentation examples, many interactive (`-it`) and intended to stay attached while a user session is open.
- Next: Report the audit result and provide a conservative maintenance command for cleaning stale exited containers without touching active long-running services.

## 2026-03-26T10:28:55Z
- Actor: user
- Intent: Ask for a tiny follow-up commit and PR update after the original harness fix had already been merged.
- Message: "faire un mini commit de suivi et mettre à jour la PR https://github.com/evaisse/the-great-analysis-challenge/pull/155."
- Action: Verified that PR `#155` was already merged, opened a dedicated follow-up issue `#156`, created a new follow-up worktree from `origin/main`, copied the updated memory log, and installed Bun dependencies in the new branch so local validation could run cleanly there too.
- Outcome: The post-merge audit note is now being published through a dedicated follow-up path instead of trying to mutate the merged PR branch.
- Next: Re-run Bun tests in the follow-up branch, commit the memory-only update, push, and open the small follow-up PR.
